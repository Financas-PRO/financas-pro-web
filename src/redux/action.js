export const setAcoes = (newState) => ({
    type: "SET ACOES",
    newState
});

export const reset = () => ({
    type: "RESET"
});

export const setAcaoSelecionada = (newState) => ({
    type: "SET ACAO SELECIONADA",
    newState
});

export const setAnalise = (newState) => ({
    type: "SET ANALISE",
    newState
})